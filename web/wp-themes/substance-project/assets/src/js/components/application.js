// inits, simple and/or common or global functions go here.


$(window).on('load resize orientationchange', function(){
// when viewport has stopped changing, do this:
	waitForFinalEvent(function(){
      // add things and stuff!
    }, 250);
});